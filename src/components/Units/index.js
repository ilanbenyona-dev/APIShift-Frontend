// const files = require.context('.', false, /\.vue$/)
// const modules = {}
// files.keys().forEach((key) => {
//      if (key === './index.js') return
//      modules[key.replace(/(\.\/|\.vue)/g, '')+'Unit'] = files(key)
//      console.log({...modules});
// })
// export default modules;